# การสะท้อนคิด สัปดาห์ที่ 4: Layered Architecture

---

## 1. สิ่งที่เรียนรู้

ได้เรียนรู้หลักการ Separation of Concerns อย่างจริงจังเป็นครั้งแรก การที่แต่ละ layer มีหน้าที่เดียวชัดเจนทำให้เข้าใจว่าทำไมโปรเจกต์จริงถึงไม่เขียนโค้ดทุกอย่างไว้ในไฟล์เดียว นอกจากนี้ยังเข้าใจว่า Controller ควรรู้แค่เรื่อง HTTP, Service รู้แค่เรื่อง business rules และ Repository รู้แค่เรื่อง database เท่านั้น

## 2. ข้อดีที่พบจากการทำจริง

เมื่อทดลองแก้ business rule เช่น เพิ่มเงื่อนไขให้งาน HIGH priority ต้องมี description พบว่าแก้แค่ `taskService.js` ไฟล์เดียวได้เลย ไม่ต้องกังวลว่าจะกระทบส่วนอื่น ต่างจากสัปดาห์ที่ 3 ที่ต้องระวังทุกบรรทัดรอบข้างเวลาแก้ไข

## 3. ความท้าทายที่พบ

ความท้าทายหลักคือการตัดสินใจว่า logic แต่ละชิ้นควรอยู่ใน layer ไหน เช่น การตรวจสอบว่า ID เป็นตัวเลขหรือไม่ควรอยู่ใน Controller หรือ Service วิธีแก้คือยึดหลักว่า Controller ตรวจสอบแค่รูปแบบข้อมูล (format) ส่วน Service ตรวจสอบ business rules เมื่อใช้หลักนี้การตัดสินใจก็ชัดเจนขึ้น

## 4. การจัดโครงสร้างโค้ด

สัปดาห์ที่ 3 เมื่อเปิด server.js ต้องไล่อ่านทั้งไฟล์เพื่อหาส่วนที่ต้องการแก้ไข แต่สัปดาห์ที่ 4 รู้ทันทีว่าต้องเปิดไฟล์ไหน ทำให้ประหยัดเวลาได้มากเมื่อโปรเจกต์ใหญ่ขึ้น

## 5. เมื่อไหร่ควรใช้ Layered Architecture

เหมาะสมเมื่อทีมมีมากกว่า 1 คน, โปรเจกต์มีแผนดูแลระยะยาว, business logic มีความซับซ้อนและเปลี่ยนแปลงบ่อย หรือต้องการเขียน unit test อย่างจริงจัง ไม่เหมาะกับ prototype ขนาดเล็กที่ต้องการความเร็วในการพัฒนา

## 6. การวิเคราะห์ Trade-offs

### ข้อดี
- แก้ไขและหาบั๊กได้ง่ายเพราะรู้ว่าต้องไปดูไฟล์ไหน
- ทดสอบแต่ละส่วนได้แยกกัน
- ทีมแบ่งงานกันได้โดยไม่ conflict

### ข้อเสีย
- โค้ดมากขึ้นและมีไฟล์มากขึ้นในช่วงแรก
- ผู้เริ่มต้นต้องใช้เวลาทำความเข้าใจโครงสร้างก่อน

### การประเมินโดยรวม
Layered Architecture เหมาะกับการพัฒนาซอฟต์แวร์จริงในระยะยาว แม้จะลงทุนเวลาในช่วงแรกมากกว่า แต่ช่วยประหยัดเวลาได้มากเมื่อโปรเจกต์เติบโตขึ้น

---
